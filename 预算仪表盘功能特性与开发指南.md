💰 预算仪表盘 (Budget Dashboard) 功能特性与开发指南

1. 产品设计哲学：为什么我们需要它？

Q1: 为什么不在对话开始时就强制询问预算？

核心理念：先造梦，再回归现实 (Dream First, Budget Later)

降低认知门槛：用户在规划初期（如“我想去巴黎”）往往对当地物价缺乏概念。如果强行询问“你有多少预算？”，用户会感到压力或填写一个不切实际的数字（如“5000元去欧洲”），导致 AI 直接无法生成高质量行程。

避免过早收敛：如果一开始就卡死预算，AI 可能会为了凑合预算而推荐劣质酒店或删减必去景点，导致用户觉得“这个 AI 不懂我”。

渐进式决策：我们允许用户先描述理想中的行程，AI 给出**“市场报价”。用户看到价格后，再根据自己的钱包进行“二次调整”**。

Q2: 预算仪表盘的作用是什么？是多余的吗？

核心定位：它是连接“行程规划”与“落地执行”的控制台。

它绝不是多余的，而是整个应用中最理性的部分。

对话框 (Chat)：负责感性规划（去哪玩，吃什么）。

仪表盘 (Dashboard)：负责理性监控（花了多少，还剩多少）。

动态反馈：它让用户直观地看到：“原来住五星级酒店会占掉我 50% 的预算，那我还是换个四星的吧”。

2. 功能详细说明

2.1 📊 仪表盘核心视图

数据来源：

总预算 (Total)：

默认值：由 AI 根据行程的“舒适型”标准自动预估（例如：AI 算出北京4天大概需要 5000 元）。

用户值：用户可以通过“编辑”按钮手动覆盖这个值。

已规划 (Planned)：

来源：AI 生成的结构化数据（budgetData）。它不是简单的文本，而是包含了住宿、餐饮、门票、交通的具体累加值。

进度条逻辑：

绿色/蓝色：预算充足（已规划 < 总预算）。

红色：预算超支（已规划 > 总预算）。此时会触发**“智能调优”**入口。

2.2 ↗️ 编辑按钮 (Edit Button)

功能：允许用户设定心理底线。

场景：AI 算出行程需要 5000 元，但用户实际只想花 3000 元。用户点击编辑 -> 输入 3000 -> 进度条变红 -> 触发“一键降级”建议。

2.3 ⬇️ 导出按钮 (Export Button)

功能：数据落地。

场景：用户确认行程后，往往需要将开销明细记录到 Excel、Notion 或记账 App 中。该按钮将结构化数据转换为文本或 CSV 格式复制到剪贴板，打通“规划”到“记账”的最后一步。

2.4 📉 智能调优 (Auto-Optimize) [新特性]

触发条件：当已规划金额 > 总预算时出现。

功能：一键触发 AI 重算。

底层逻辑：向 AI 发送指令：“保持天数和景点不变，但将住宿标准从‘豪华’降为‘经济’，将‘专车’改为‘地铁’，以满足 [X] 元的预算限制。”

3. 开发指导 (Development Guide)

3.1 数据结构 (JSON Schema)

后端（AI）返回的数据必须严格遵循此格式，以驱动前端渲染：

{
  "budget_data": {
    "total_range": "3000-4000 CNY",
    "total_amount": 3372,        // [关键] 用于计算进度条分母
    "currency": "CNY",
    "categories": [
      { 
        "name": "住宿 (3晚)", 
        "amount": 1032, 
        "desc": "汉庭/如家等经济型酒店" 
      },
      { 
        "name": "门票", 
        "amount": 860, 
        "desc": "环球影城 + 故宫" 
      },
      { 
        "name": "交通", 
        "amount": 280, 
        "desc": "地铁为主，少量打车" 
      }
    ],
    "saving_tip": "建议办理京津冀旅游一卡通，可节省约 150 元门票费。"
  }
}


3.2 交互逻辑实现

A. 初始化 (Init)

用户在左侧对话框确认行程。

前端自动调用（或用户点击）handleEstimateBudget。

AI 返回 JSON 数据，填充 budgetData 状态。

若用户未手动设置过预算，将 budgetData.total_amount * 1.2 作为默认总预算（留 20% 余地）。

B. 用户修改预算 (User Edit)

const handleEdit = () => {
    // 1. 获取用户输入
    const newBudget = prompt("请输入您的总预算", totalBudget);
    // 2. 更新状态
    setUserBudget(Number(newBudget));
    // 3. 自动计算差额
    // (渲染逻辑会自动更新进度条颜色)
};


C. 智能降级 (Auto-Downgrade)

这是连接仪表盘与行程生成的关键闭环：

const handleAutoOptimize = (targetBudget) => {
    // 1. 构建 Prompt
    const instruction = `
        当前行程预算超支。
        目标预算：${targetBudget} 元。
        请重新规划行程：
        - 保持游玩景点不变
        - 将酒店更换为更经济的选项（如青旅或快捷酒店）
        - 交通方式改为纯公共交通
        - 重新计算总价
    `;
    
    // 2. 将指令发送给 AI (复用聊天接口)
    handleSend(instruction);
};


3.3 UI 适配规范

宽屏 (Desktop)：仪表盘常驻于右侧 SmartSidebar 底部，作为全局监控。

移动端 (Mobile)：仪表盘折叠在“智囊”Tab 中，作为独立卡片展示。

4. 总结

预算仪表盘不是一个静态的“账单”，而是一个动态的“调节器”。
它解决了“想要最好的体验”和“只有有限的钱”之间的矛盾，通过 AI 的实时计算和调整，帮助用户找到二者的平衡点。