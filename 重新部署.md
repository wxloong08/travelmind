# 1. 完全停止并清理（重要！）
docker-compose -f docker-compose.yml down -v

# 2. 解压新的项目文件（覆盖旧的）

# 3. 删除旧镜像，强制重新构建
docker rmi travelmind-app 2>$null

# 4. 重新构建（必须 --no-cache）
docker-compose -f docker-compose.yml build --no-cache

# 5. 启动服务
docker-compose -f docker-compose.yml up -d

# 6. 查看启动状态（Langfuse 需要 1-2 分钟）
docker-compose -f docker-compose.yml ps

# 等待直到看到：
#   travelmind-langfuse-db  healthy
#   travelmind-langfuse     healthy  
#   travelmind              healthy

# 7. 查看日志确认 Langfuse SDK 加载成功
docker-compose -f docker-compose.yml logs app | grep -i langfuse
# 应该看到: "Langfuse SDK loaded successfully"

# 8. 重启服务
docker restart travelmind